#ifndef SMPENGINE_GLOBALS_H
#define SMPENGINE_GLOBALS_H
char *G_RESOURCE_PATH;

char *G_DEFAULT_TEXTURE_PATH;

char *G_DEFAULT_FONT_PATH;







// Macros:
#define ABS_VAL(NUM) ((NUM < 0) ? (0 - NUM) : (NUM))


#endif
